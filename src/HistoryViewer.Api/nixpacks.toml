# Disable auto-detection, use explicit .NET 8
providers = []

[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "dotnet restore HistoryViewer.Api.csproj",
  "dotnet publish HistoryViewer.Api.csproj -c Release -o out"
]

[start]
cmd = "dotnet out/HistoryViewer.Api.dll"

